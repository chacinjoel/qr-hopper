import {parsePacket} from './protocol.js';
let reader=null;
async function loadReader(){if(reader)return reader;const mod=await import('./vendor/zxing/es/reader/index.js');mod.prepareZXingModule({overrides:{locateFile:path=>new URL('./vendor/zxing/reader/'+path,self.location.href).href}});reader=mod;return reader;}
self.onmessage=async e=>{const m=e.data;try{if(m.type==='init'){const mod=await loadReader();self.postMessage({type:'ready',version:mod.ZXING_WASM_VERSION||'3.1.4'});return;}if(m.type!=='decode')return;const mod=await loadReader(),imageData=new ImageData(new Uint8ClampedArray(m.rgba),m.width,m.height),results=await mod.readBarcodes(imageData,{formats:['QRCode'],maxNumberOfSymbols:2,tryHarder:false,tryRotate:true,tryInvert:false,returnErrors:false,textMode:'Plain'}),packets=[];for(const r of results){const bytes=r.bytes instanceof Uint8Array?r.bytes:new Uint8Array(r.bytes||0),p=parsePacket(bytes);if(!p)continue;packets.push({header:{session:p.session,seq:p.seq,k:p.k,blockLen:p.blockLen,totalLen:p.totalLen,degree:p.degree,codeIndex:p.codeIndex,seed:p.seed,systematic:p.systematic},payload:p.payload.buffer,position:r.position,rotation:r.rotation});}self.postMessage({type:'decoded',id:m.id,packets},packets.map(p=>p.payload));}catch(error){self.postMessage({type:'error',id:m?.id,message:error?.message||String(error)});}};
