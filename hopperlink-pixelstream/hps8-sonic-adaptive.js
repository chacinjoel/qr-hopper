(() => {
'use strict';

const VERSION='0.10.2',MAGIC=[0x53,0x38],PROTO=1;
const TYPE={ACK:1,PROGRESS:2,BLOOM:3,COMPLETE:4};
const PROFILES={
  high:{name:'HIGH',freq:[17400,18000,18600,19200],audibility:'near-ultrasonic'},
  mid:{name:'MID',freq:[13800,14400,15000,15600],audibility:'high-frequency'},
  safe:{name:'SAFE',freq:[10800,11600,12400,13200],audibility:'compatibility'}
};
const PROFILE_ORDER=['high','mid','safe'];
const PREAMBLE=new Uint8Array([0xF0,0xA5,0xF0,0xA5]);
const TONE_MS=22,GAP_MS=22,LEAD_MS=120,PROFILE_GAP_MS=160,TAIL_MS=80,POLL_MS=6;
let outCtx=null,micCtx=null,micStream=null,micSource=null,analyser=null,silentGain=null,listenTimer=null,listenCb=null;

function u16(n){return[(n>>>8)&255,n&255];}function u32(n){return[(n>>>24)&255,(n>>>16)&255,(n>>>8)&255,n&255];}
function readU16(a,o){return(a[o]<<8)|a[o+1];}function readU32(a,o){return((((a[o]<<24)>>>0)|(a[o+1]<<16)|(a[o+2]<<8)|a[o+3])>>>0);}
function crc8(a){let c=0x5a;for(const b of a){c^=b;for(let i=0;i<8;i++)c=(c&0x80)?((c<<1)^0x07)&255:(c<<1)&255;}return c;}
function concat(...xs){const n=xs.reduce((s,a)=>s+a.length,0),o=new Uint8Array(n);let p=0;for(const a of xs){o.set(a,p);p+=a.length;}return o;}
function pack(type,session,payload=new Uint8Array(0)){payload=payload instanceof Uint8Array?payload:new Uint8Array(payload);if(payload.length>64)throw new Error('SonicLink payload >64 B');const h=new Uint8Array(9);h.set(MAGIC,0);h[2]=PROTO;h[3]=type;h.set(u32(session>>>0),4);h[8]=payload.length;const body=concat(h,payload);return concat(body,new Uint8Array([crc8(body)]));}
function unpack(a,off=0){if(a.length-off<10)return{pending:true};if(a[off]!==MAGIC[0]||a[off+1]!==MAGIC[1]||a[off+2]!==PROTO)return null;const len=a[off+8],need=10+len;if(a.length-off<need)return{pending:true,need};const body=a.slice(off,off+9+len),expected=a[off+9+len];if(crc8(body)!==expected)return{bad:true,need};return{ok:true,need,type:a[off+3],session:readU32(a,off+4),payload:a.slice(off+9,off+9+len)};}
function bytesToSymbols(bytes){const out=[];for(const b of bytes)out.push((b>>>6)&3,(b>>>4)&3,(b>>>2)&3,b&3);return out;}
function symbolsToBytes(sym){const n=Math.floor(sym.length/4),out=new Uint8Array(n);for(let i=0;i<n;i++)out[i]=(sym[i*4]<<6)|(sym[i*4+1]<<4)|(sym[i*4+2]<<2)|sym[i*4+3];return out;}
const PRE_SYM=bytesToSymbols(PREAMBLE);
function emit(name,detail={}){try{window.dispatchEvent(new CustomEvent('hopper:hps8-sonic-'+name,{detail}));}catch{}}
function log(msg){const e=document.getElementById('sendLog');if(e)e.textContent=`[${new Date().toLocaleTimeString()}] SONIC8A · ${msg}\n`+e.textContent.slice(0,8500);}
function status(text,kind=''){const e=document.getElementById('hps8SonicStatus');if(e){e.textContent=text;e.className='chip '+kind;}}
function overlay(text,kind=''){let e=document.getElementById('sonicOverlayDiag');const o=document.getElementById('streamOverlay');if(!o)return;if(!e){e=document.createElement('div');e.id='sonicOverlayDiag';o.appendChild(e);}e.textContent=text;e.dataset.kind=kind;}

async function ensureOutput(){const AC=window.AudioContext||window.webkitAudioContext;if(!AC)throw new Error('Web Audio no disponible');if(!outCtx)outCtx=new AC({latencyHint:'interactive'});if(outCtx.state==='suspended')await outCtx.resume();return outCtx;}
function chooseProfiles(repeats=2,requested){if(Array.isArray(requested)&&requested.length)return requested.filter(x=>PROFILES[x]);const r=Math.max(1,repeats|0);if(r===1)return['mid'];if(r===2)return['high','mid'];const out=['high','mid','safe'];for(let i=3;i<r;i++)out.push(i%2?'mid':'safe');return out;}
function symbolSchedule(symbols){return symbols.map(sym=>({sym,toneMs:TONE_MS,gapMs:GAP_MS}));}
function buildBuffer(symbols,profiles){const ctx=outCtx,sr=ctx.sampleRate,toneN=Math.round(sr*TONE_MS/1000),gapN=Math.round(sr*GAP_MS/1000),leadN=Math.round(sr*LEAD_MS/1000),profGapN=Math.round(sr*PROFILE_GAP_MS/1000),tailN=Math.round(sr*TAIL_MS/1000),perProfile=symbols.length*(toneN+gapN),total=leadN+profiles.length*perProfile+Math.max(0,profiles.length-1)*profGapN+tailN,b=ctx.createBuffer(1,total,sr),d=b.getChannelData(0),fade=Math.max(6,Math.round(sr*.0016));let p=leadN;for(let pi=0;pi<profiles.length;pi++){const prof=PROFILES[profiles[pi]]||PROFILES.mid;for(const sym of symbols){const f=prof.freq[sym];for(let i=0;i<toneN;i++){const env=Math.min(1,i/fade,(toneN-1-i)/fade),t=i/sr;d[p+i]=.16*env*Math.sin(2*Math.PI*f*t);}p+=toneN+gapN;}if(pi<profiles.length-1)p+=profGapN;}return b;}
async function send(type,session,payload,{repeats=2,label='CTRL',profiles=null}={}){await ensureOutput();const packet=pack(type,session,payload),symbols=bytesToSymbols(new Uint8Array([...PREAMBLE,...packet])),prof=chooseProfiles(repeats,profiles),buf=buildBuffer(symbols,prof),src=outCtx.createBufferSource();src.buffer=buf;src.connect(outCtx.destination);status(`SONIC · TX ${label}`,'mid');overlay(`SONIC TX · ${label} · ${prof.map(p=>PROFILES[p].name).join('→')}`,'tx');emit('tx',{type,session,bytes:packet.length,durationMs:buf.duration*1000,label,profiles:prof.slice()});log(`TX ${label} · ${packet.length} B · bandas ${prof.join('→')} · ${buf.duration.toFixed(1)} s`);return new Promise((resolve,reject)=>{src.onended=()=>{status('SONIC · LISTO','on');overlay('SONIC · TX terminado','ok');resolve();};try{src.start();}catch(e){reject(e);}});}

function dbAt(freq,f,sr,fft){const bin=Math.round(f*fft/sr);return freq[Math.max(0,Math.min(freq.length-1,bin))]??-120;}
function median(a){const s=a.slice().sort((x,y)=>x-y);return s[Math.floor(s.length/2)]??-120;}
function stopListener(){if(listenTimer){clearInterval(listenTimer);listenTimer=null;}try{micStream?.getTracks?.().forEach(t=>t.stop());}catch{}try{micSource?.disconnect?.();analyser?.disconnect?.();silentGain?.disconnect?.();}catch{}try{micCtx?.close?.();}catch{}micCtx=null;micStream=null;micSource=null;analyser=null;silentGain=null;listenCb=null;status('SONIC · OFF','');overlay('SONIC RX · OFF','');}
async function startListener(onMessage){stopListener();if(!navigator.mediaDevices?.getUserMedia)throw new Error('Micrófono no disponible');const stream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:false,noiseSuppression:false,autoGainControl:false,channelCount:1,sampleRate:{ideal:48000}},video:false}),AC=window.AudioContext||window.webkitAudioContext;if(!AC)throw new Error('Web Audio no disponible');micCtx=new AC({latencyHint:'interactive'});if(micCtx.state==='suspended')await micCtx.resume();micStream=stream;micSource=micCtx.createMediaStreamSource(stream);analyser=micCtx.createAnalyser();analyser.fftSize=512;analyser.smoothingTimeConstant=0;analyser.minDecibels=-110;analyser.maxDecibels=-8;silentGain=micCtx.createGain();silentGain.gain.value=1e-7;micSource.connect(analyser);analyser.connect(silentGain);silentGain.connect(micCtx.destination);listenCb=onMessage;const sr=micCtx.sampleRate,fft=analyser.fftSize,freq=new Float32Array(analyser.frequencyBinCount),symbols=[],profiles=[];let activeKey='',activeSym=-1,activeProfile='',hits=0,quiet=0,lastDiag=0,lastPacketKey='',lastPacketTs=0;status('SONIC · ESCUCHANDO','on');overlay('SONIC RX · DSP ACTIVO · esperando señal…','listen');const track=stream.getAudioTracks?.()[0],settings=track?.getSettings?.()||{};log(`RX adaptativo · ${sr} Hz · FFT ${fft} · guard ${TONE_MS}/${GAP_MS} ms · EC ${settings.echoCancellation??'?'} NS ${settings.noiseSuppression??'?'} AGC ${settings.autoGainControl??'?'}`);
  function feed(sym,profile){symbols.push(sym);profiles.push(profile);if(symbols.length>1800){symbols.splice(0,900);profiles.splice(0,900);}scan();}
  function findPreamble(){outer:for(let i=0;i<=symbols.length-PRE_SYM.length;i++){for(let j=0;j<PRE_SYM.length;j++)if(symbols[i+j]!==PRE_SYM[j])continue outer;return i;}return-1;}
  function majority(arr){const m={};for(const x of arr)m[x]=(m[x]||0)+1;return Object.entries(m).sort((a,b)=>b[1]-a[1])[0]?.[0]||'unknown';}
  function scan(){while(true){const p=findPreamble();if(p<0){if(symbols.length>PRE_SYM.length*3){symbols.splice(0,symbols.length-PRE_SYM.length*2);profiles.splice(0,profiles.length-PRE_SYM.length*2);}return;}if(p>0){symbols.splice(0,p);profiles.splice(0,p);}const body=symbols.slice(PRE_SYM.length),bytes=symbolsToBytes(body),r=unpack(bytes,0);if(r?.pending)return;if(!r||r.bad){symbols.shift();profiles.shift();continue;}const consumed=PRE_SYM.length+r.need*4,profile=majority(profiles.slice(0,consumed)),key=`${r.session}:${r.type}:${crc8(r.payload||[])}`;symbols.splice(0,consumed);profiles.splice(0,consumed);const now=performance.now();if(key===lastPacketKey&&now-lastPacketTs<900)continue;lastPacketKey=key;lastPacketTs=now;status(`SONIC · RX ${r.type} · ${profile.toUpperCase()}`,'on');overlay(`SONIC RX ✓ · TYPE ${r.type} · ${profile.toUpperCase()} · CRC OK`,'ok');emit('rx',{type:r.type,session:r.session,bytes:r.need,profile});try{listenCb?.({...r,profile});}catch(e){console.error(e);}}}
  listenTimer=setInterval(()=>{if(!analyser||!micCtx)return;analyser.getFloatFrequencyData(freq);const candidates=[];for(const [pk,p] of Object.entries(PROFILES))for(let si=0;si<4;si++)candidates.push({profile:pk,sym:si,db:dbAt(freq,p.freq[si],sr,fft),hz:p.freq[si]});candidates.sort((a,b)=>b.db-a.db);const best=candidates[0],second=candidates[1],floor=median(candidates.map(x=>x.db)),margin=best.db-second.db,snr=best.db-floor,tone=best.db>-82&&margin>3.5&&snr>7?best:null;if(tone){quiet=0;const key=`${tone.profile}:${tone.sym}`;if(activeKey===key)hits++;else{if(activeKey&&hits>=2)feed(activeSym,activeProfile);activeKey=key;activeSym=tone.sym;activeProfile=tone.profile;hits=1;}}else{quiet++;if(activeKey&&quiet>=2){if(hits>=2)feed(activeSym,activeProfile);activeKey='';activeSym=-1;activeProfile='';hits=0;quiet=0;}}const now=performance.now();if(now-lastDiag>220){lastDiag=now;const label=tone?`${PROFILES[tone.profile].name} ${(tone.hz/1000).toFixed(1)}kHz ${tone.db.toFixed(0)}dB Δ${margin.toFixed(1)}`:`sin tono · pico ${best.db.toFixed(0)}dB`;overlay(`SONIC RX · ${label}`,(tone?'signal':'listen'));emit('spectrum',{profile:tone?.profile||null,hz:tone?.hz||best.hz,bestDb:best.db,margin,snr,floor,settings});}},POLL_MS);
  emit('listener-ready',{sampleRate:sr,fft,profiles:Object.fromEntries(Object.entries(PROFILES).map(([k,v])=>[k,v.freq.slice()])),settings,guardedSymbols:true});return true;}

function progressPayload(permille,missing,flags=0){return new Uint8Array([...u16(Math.max(0,Math.min(1000,permille))),...u16(Math.max(0,Math.min(65535,missing))),flags&255]);}
function parseProgress(p){return p.length>=5?{permille:readU16(p,0),missing:readU16(p,2),flags:p[4]}:null;}
function bloomPayload(missing,bloom){return concat(new Uint8Array(u16(Math.min(65535,missing))),bloom);}
function parseBloom(p){return p.length>=6?{missing:readU16(p,0),bloom:p.slice(2)}:null;}
function completePayload(fileCrc){return new Uint8Array(u32(fileCrc>>>0));}
function parseComplete(p){return p.length>=4?{fileCrc:readU32(p,0)}:null;}

window.__hopperHPS8Sonic={version:VERSION,type:TYPE,frequencies:PROFILES.high.freq.slice(),profiles:PROFILES,adaptiveBands:true,guardedSymbols:true,nearUltrasonic:true,timing:{toneMs:TONE_MS,gapMs:GAP_MS,pollMs:POLL_MS,fftSize:512},pack,unpack,bytesToSymbols,symbolsToBytes,symbolSchedule,ensureOutput,startListener,stopListener,send,progressPayload,parseProgress,bloomPayload,parseBloom,completePayload,parseComplete};
})();
