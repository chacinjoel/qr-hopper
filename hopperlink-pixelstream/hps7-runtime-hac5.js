(() => {
'use strict';

const CORE_URL='./hps7.js?v=090core';

function replaceOne(src,label,from,to){
  if(!src.includes(from)) throw new Error(`HPS7 runtime patch missing: ${label}`);
  const out=src.replace(from,to);
  if(out===src) throw new Error(`HPS7 runtime patch failed: ${label}`);
  return out;
}

function patchCore(src){
  src=replaceOne(src,'dense-logical-blocks',"const BASE_CHUNK = 360;","const BASE_CHUNK = 372;");
  src=replaceOne(src,'repair-500',"function repairPasses(n){return n<=100?3:n<=300?2:1;}","function repairPasses(n){return n>0&&n<=500?3:1;}");
  src=replaceOne(src,'rx-telemetry-state',"function freshRx(){return{session:null,total:0,dataGrid:56,preferredBits:4,lastBits:4,nextBits:null,autoMod:true,fileCrc:0,fileSize:0,chunks:new Map(),errors:0,lastPassRound:-1,round:0,startedAt:0,lastCount:0,lastTs:0,emaRate:0,complete:null,passStartCount:0,lastPassReceived:0};}","function freshRx(){return{session:null,total:0,dataGrid:56,preferredBits:4,lastBits:4,nextBits:null,autoMod:true,controlMode:'manual',fileCrc:0,fileSize:0,chunks:new Map(),errors:0,lastPassRound:-1,round:0,startedAt:0,lastCount:0,lastTs:0,emaRate:0,complete:null,passStartCount:0,lastPassReceived:0,sessionStartedAt:0,firstDataAt:0,completedAt:0,bytesReceived:0,lastBytes:0,byteRateEma:0,peakByteRate:0,validDataPackets:0,duplicateBlocks:0,roundStartedAt:0,lastDataRound:-1,roundStats:[]};}");
  src=replaceOne(src,'metric-vsync-control',"function clamp(v,a,b){return Math.max(a,Math.min(b,v));}","function clamp(v,a,b){return Math.max(a,Math.min(b,v));}\nfunction emitMetric(name,detail={}){try{window.dispatchEvent(new CustomEvent('hopper:'+name,{detail}));}catch{}}\nfunction opticalHold(ms){return new Promise(resolve=>{const start=performance.now();const step=t=>{if(t-start>=ms-1)resolve();else requestAnimationFrame(step);};requestAnimationFrame(step);});}\nfunction controlModeSelection(){return $('controlMode')?.value==='acoustic'?'acoustic':'manual';}");
  src=replaceOne(src,'micro-cell-guard',"function renderPacket(raw,grid,bits){const c=$('pixelCanvas');c.width=grid;c.height=grid;const ctx=c.getContext('2d',{alpha:false}),sym=rawToSymbols(raw,grid,bits),pal=palette(bits),img=ctx.createImageData(grid,grid);for(let i=0;i<sym.length;i++){const rgb=pal[sym[i]]||pal[0],p=i*4;img.data[p]=rgb[0];img.data[p+1]=rgb[1];img.data[p+2]=rgb[2];img.data[p+3]=255;}ctx.putImageData(img,0,0);}","function renderPacket(raw,grid,bits){const c=$('pixelCanvas'),sym=rawToSymbols(raw,grid,bits),pal=palette(bits);if(bits===2){c.width=grid;c.height=grid;const ctx=c.getContext('2d',{alpha:false}),img=ctx.createImageData(grid,grid);for(let i=0;i<sym.length;i++){const rgb=pal[sym[i]]||pal[0],p=i*4;img.data[p]=rgb[0];img.data[p+1]=rgb[1];img.data[p+2]=rgb[2];img.data[p+3]=255;}ctx.putImageData(img,0,0);return;}const S=8,P=1,W=grid*S;c.width=W;c.height=W;const ctx=c.getContext('2d',{alpha:false}),img=ctx.createImageData(W,W),d=img.data;for(let p=0;p<d.length;p+=4){d[p]=58;d[p+1]=58;d[p+2]=58;d[p+3]=255;}for(let gy=0;gy<grid;gy++)for(let gx=0;gx<grid;gx++){const rgb=pal[sym[gy*grid+gx]]||pal[0],x0=gx*S+P,y0=gy*S+P;for(let y=y0;y<(gy+1)*S-P;y++)for(let x=x0;x<(gx+1)*S-P;x++){const p=(y*W+x)*4;d[p]=rgb[0];d[p+1]=rgb[1];d[p+2]=rgb[2];}}ctx.putImageData(img,0,0);}");
  src=replaceOne(src,'prepare-acoustic',"cancelProtocol(false);const f=$('fileInput')?.files?.[0];","cancelProtocol(false);const controlMode=controlModeSelection();if(controlMode==='acoustic'){if(!window.HopperAcoustic)throw new Error('Canal acústico no cargado.');await window.HopperAcoustic.ensureSenderMic();}else window.HopperAcoustic?.stopMic?.();const f=$('fileInput')?.files?.[0];");
  src=replaceOne(src,'tx-control-mode',"tx={session:randomId(),grid,preferredBits:mod.bits,activeBits:mod.bits,autoMod:mod.auto,f,fileBytes,fileCrc:crc32(fileBytes),chunks,total:chunks.length,round:0,pendingMissing:null,repairBits:mod.bits};","tx={session:randomId(),grid,preferredBits:mod.bits,activeBits:mod.bits,autoMod:mod.auto,controlMode,f,fileBytes,fileCrc:crc32(fileBytes),chunks,total:chunks.length,round:0,pendingMissing:null,repairBits:mod.bits};");
  src=replaceOne(src,'hello-payload-control',"function helloPayload(){return concat(new Uint8Array([tx.grid,tx.preferredBits,tx.autoMod?1:0]),new Uint8Array(u16(BASE_CHUNK)),new Uint8Array(u32(tx.fileCrc)),new Uint8Array(u32(tx.fileBytes.length)));}","function helloPayload(){return concat(new Uint8Array([tx.grid,tx.preferredBits,tx.autoMod?1:0,tx.controlMode==='acoustic'?1:0]),new Uint8Array(u16(BASE_CHUNK)),new Uint8Array(u32(tx.fileCrc)),new Uint8Array(u32(tx.fileBytes.length)));}");
  src=replaceOne(src,'closing-burst-order',"for(let pass=1;pass<=passes;pass++){const repeats=pass===1?sp.repeat:1;for(let gi=0;gi<groups.length;gi++){const g=groups[gi],raw=makePacket(TYPE.DATA,tx.session,round,g.first,tx.total,g.payload,tx.grid,bits);","for(let pass=1;pass<=passes;pass++){const repeats=pass===1?sp.repeat:1,passGroups=pass===1?groups:(pass===2?groups.slice().reverse():groups.filter((_,i)=>i%2===0).concat(groups.filter((_,i)=>i%2===1)));if(isRepair&&passes===3){slog(`Closing Burst ciclo ${pass}/3 · ${indices.length} faltantes · orden ${pass===1?'normal':pass===2?'invertido':'intercalado'}.`);emitMetric('closing-cycle',{cycle:pass,cycles:3,missing:indices.length,bits});}for(let gi=0;gi<passGroups.length;gi++){const g=passGroups[gi],raw=makePacket(TYPE.DATA,tx.session,round,g.first,tx.total,g.payload,tx.grid,bits);");
  src=replaceOne(src,'vsync-pacing',"await sleep(frameMs);}}}","await opticalHold(frameMs);}}}");
  src=replaceOne(src,'show-pass-end-acoustic',"function showPassEnd(round,bits){txState=TXS.PASS_END;const raw=makePacket(TYPE.PASS_END,tx.session,round,0,tx.total,passEndPayload(bits),CONTROL_GRID,2);openOverlay(`PASS_END · R${round+1}`,'Control vuelve a gris 2-bit. Espera NACK/COMPLETE.','Receptor mostró control · Leer NACK/COMPLETE',()=>listenForControl(round));renderPacket(raw,CONTROL_GRID,2);setPhase('EMISOR · PASS_END ROBUSTO','mid');slog(`PASS_END · última ronda ${bits}-bit.`);}","function showPassEnd(round,bits){txState=TXS.PASS_END;const raw=makePacket(TYPE.PASS_END,tx.session,round,0,tx.total,passEndPayload(bits),CONTROL_GRID,2);if(tx.controlMode==='acoustic'){openOverlay(`PASS_END · R${round+1}`,'HAC5 automático: micrófono + FlashSync opcional esperan NACK/COMPLETE.',null,null);renderPacket(raw,CONTROL_GRID,2);setPhase('EMISOR · HAC5 ESCUCHANDO','mid');slog(`PASS_END HAC5 · ronda ${round+1}.`);setTimeout(()=>listenForAcousticControl(round),0);}else{openOverlay(`PASS_END · R${round+1}`,'Control vuelve a gris 2-bit. Espera NACK/COMPLETE.','Receptor mostró control · Leer NACK/COMPLETE',()=>listenForControl(round));renderPacket(raw,CONTROL_GRID,2);setPhase('EMISOR · PASS_END ROBUSTO','mid');slog(`PASS_END · última ronda ${bits}-bit.`);}}\nasync function listenForAcousticControl(round){try{const r=await window.HopperAcoustic.listenControl({session:tx.session,round,timeoutMs:120000});if(r.type===window.HopperAcoustic.TYPE.NACK){if(r.total!==tx.total||(r.fileCrc&&r.fileCrc!==tx.fileCrc)||!BITS_OPTIONS.includes(r.bits))throw new Error('NACK HAC5 no coincide con la sesión');tx.pendingMissing=r.missing.slice();tx.round=round;tx.repairBits=tx.autoMod?r.bits:tx.preferredBits;txState=TXS.NACK_READY;emitMetric('acoustic-control',{type:'NACK',missing:r.missing.length,payloadBytes:r.payloadBytes,round:round+1});slog(`NACK HAC5 OK · ${r.missing.length} faltantes · repair ${tx.repairBits}-bit.`);setPhase(`EMISOR · HAC5 NACK ${r.missing.length}`,'on');closeOverlay();await sleep(180);sendRepair();}else{const ok=r.total===tx.total&&r.fileCrc===tx.fileCrc;if(!ok)throw new Error('COMPLETE HAC5 inválido');txState=TXS.DONE;setPhase('EMISOR · COMPLETADO HAC5','on');slog('COMPLETE HAC5 confirmado por CRC.');emitMetric('acoustic-control',{type:'COMPLETE',round:round+1});window.HopperAcoustic.stopMic();closeOverlay();}}catch(e){slog(`HAC5 falló: ${e.message}. Fallback manual disponible.`);emitMetric('acoustic-fallback',{round:round+1,error:e.message});openOverlay(`PASS_END · R${round+1} · fallback`,'No se pudo validar HAC5. Puedes usar NACK visual sin reiniciar.','Usar NACK manual',()=>listenForControl(round));setPhase('EMISOR · FALLBACK MANUAL','mid');}}");
  src=replaceOne(src,'reset-session-time',"function resetRxSession(session,total,dataGrid,bits,autoMod,fileCrc=0,fileSize=0){rx=freshRx();rx.session=session;rx.total=total;rx.dataGrid=dataGrid;rx.preferredBits=bits;rx.lastBits=bits;rx.autoMod=autoMod;rx.fileCrc=fileCrc;rx.fileSize=fileSize;$('rxFrames').textContent='0';$('rxTotal').textContent=total;$('rxMissing').textContent=total;$('rxBar').style.width='0%';$('receivedBox').style.display='none';trackedH=null;trackedMarkers=null;trackedFails=0;trackedSuccess=0;markerMemory=null;}","function resetRxSession(session,total,dataGrid,bits,autoMod,fileCrc=0,fileSize=0){rx=freshRx();rx.session=session;rx.total=total;rx.dataGrid=dataGrid;rx.preferredBits=bits;rx.lastBits=bits;rx.autoMod=autoMod;rx.fileCrc=fileCrc;rx.fileSize=fileSize;rx.sessionStartedAt=performance.now();$('rxFrames').textContent='0';$('rxTotal').textContent=total;$('rxMissing').textContent=total;$('rxBar').style.width='0%';$('receivedBox').style.display='none';trackedH=null;trackedMarkers=null;trackedFails=0;trackedSuccess=0;markerMemory=null;}");
  src=replaceOne(src,'handle-hello-control',"function handleHello(p){if(cameraMode!=='receiverData'||p.grid!==CONTROL_GRID||p.bits!==2||p.payload.length<13)return;const dataGrid=p.payload[0],bits=p.payload[1],autoMod=!!p.payload[2],base=readU16(p.payload,3),fileCrc=readU32(p.payload,5),fileSize=readU32(p.payload,9);if(!DATA_GRIDS.includes(dataGrid)||!BITS_OPTIONS.includes(bits)||base!==BASE_CHUNK)return;if(rx.session===null){resetRxSession(p.session,p.total,dataGrid,bits,autoMod,fileCrc,fileSize);rxState=RXS.LOCKED;rx.passStartCount=0;rlog(`HPS7 sesión bloqueada · ${p.total} bloques · grid ${dataGrid} · ${bits}-bit${autoMod?' adaptativo':''} · ${fmtBytes(fileSize)}.`);setPhase(`RECEPTOR · LOCK ${bits}-BIT`,'on');}else if(rx.session!==p.session)rlog('HELLO de otra sesión ignorado.');}","function handleHello(p){if(cameraMode!=='receiverData'||p.grid!==CONTROL_GRID||p.bits!==2||p.payload.length<14)return;const dataGrid=p.payload[0],bits=p.payload[1],autoMod=!!p.payload[2],controlMode=p.payload[3]===1?'acoustic':'manual',base=readU16(p.payload,4),fileCrc=readU32(p.payload,6),fileSize=readU32(p.payload,10);if(!DATA_GRIDS.includes(dataGrid)||!BITS_OPTIONS.includes(bits)||base!==BASE_CHUNK)return;if(rx.session===null){resetRxSession(p.session,p.total,dataGrid,bits,autoMod,fileCrc,fileSize);rx.controlMode=controlMode;rxState=RXS.LOCKED;rx.passStartCount=0;rlog(`HPS7 sesión bloqueada · ${p.total} bloques · grid ${dataGrid} · ${bits}-bit · control ${controlMode}.`);setPhase(`RECEPTOR · LOCK ${bits}-BIT · ${controlMode==='acoustic'?'HAC5':'MANUAL'}`,'on');if($('controlChannelStatus')){$('controlChannelStatus').textContent=controlMode==='acoustic'?'HAC5 · SESIÓN':'NACK MANUAL';$('controlChannelStatus').className='chip on';}emitMetric('hello',{session:p.session,total:p.total,fileSize,dataGrid,bits,autoMod,baseChunk:BASE_CHUNK,controlMode});}else if(rx.session!==p.session)rlog('HELLO de otra sesión ignorado.');}");
  src=replaceOne(src,'data-telemetry',"function handleData(p){if(cameraMode!=='receiverData'||rx.session===null)return;if(p.session!==rx.session||p.total!==rx.total||p.grid!==rx.dataGrid||!BITS_OPTIONS.includes(p.bits)){rx.errors++;updateErrors();return;}const bundle=parseBundle(p.payload,rx.total);if(!bundle){rx.errors++;updateErrors();return;}rxState=RXS.RECEIVING;rx.round=Math.max(rx.round,p.round);rx.lastBits=p.bits;rx.nextBits=null;let added=0;for(const e of bundle)if(!rx.chunks.has(e.idx)){rx.chunks.set(e.idx,e.data);added++;}if(added){$('rxFrames').textContent=rx.chunks.size;$('rxMissing').textContent=Math.max(0,rx.total-rx.chunks.size);$('rxBar').style.width=((rx.chunks.size/rx.total)*100).toFixed(1)+'%';updateMetrics();}setPhase(`RECEPTOR · ${p.bits}-BIT · ${rx.chunks.size}/${rx.total}`,'on');}","function handleData(p){if(cameraMode!=='receiverData'||rx.session===null)return;if(p.session!==rx.session||p.total!==rx.total||p.grid!==rx.dataGrid||!BITS_OPTIONS.includes(p.bits)){rx.errors++;updateErrors();emitMetric('reject',{errors:rx.errors,reason:'identity'});return;}const bundle=parseBundle(p.payload,rx.total);if(!bundle){rx.errors++;updateErrors();emitMetric('reject',{errors:rx.errors,reason:'bundle'});return;}rxState=RXS.RECEIVING;rx.round=Math.max(rx.round,p.round);rx.lastBits=p.bits;rx.nextBits=null;const ts=performance.now();if(!rx.firstDataAt)rx.firstDataAt=ts;if(rx.lastDataRound!==p.round){rx.lastDataRound=p.round;rx.roundStartedAt=ts;}let added=0,addedBytes=0,dups=0;for(const e of bundle){if(!rx.chunks.has(e.idx)){rx.chunks.set(e.idx,e.data);added++;addedBytes+=e.data.length;}else dups++;}rx.validDataPackets++;rx.duplicateBlocks+=dups;if(added){rx.bytesReceived+=addedBytes;$('rxFrames').textContent=rx.chunks.size;$('rxMissing').textContent=Math.max(0,rx.total-rx.chunks.size);$('rxBar').style.width=((rx.chunks.size/rx.total)*100).toFixed(1)+'%';updateMetrics();}emitMetric('data',{round:p.round+1,bits:p.bits,added,addedBytes,duplicates:dups,count:rx.chunks.size,total:rx.total,bytesReceived:rx.bytesReceived,validPackets:rx.validDataPackets,errors:rx.errors});setPhase(`RECEPTOR · ${p.bits}-BIT · ${rx.chunks.size}/${rx.total}`,'on');}");
  src=replaceOne(src,'passend-acoustic-branch',"const{missing}=makeMissingBitmap(),suggest=suggestedRepairBits(missing,lastBits),gained=Math.max(0,rx.chunks.size-rx.passStartCount);rlog(`PASS_END R${p.round+1}: +${gained} bloques · ${rx.chunks.size}/${rx.total} · faltan ${missing} · siguiente ${suggest}-bit.`);rx.passStartCount=rx.chunks.size;stopCamera();if(missing===0){const assembled=assembleFile();if(!assembled){rlog('CRC/ensamblado falló; solicitando retransmisión robusta completa.');rx.chunks.clear();$('rxFrames').textContent='0';$('rxMissing').textContent=rx.total;$('rxBar').style.width='0%';showNack(p.round,2);}else showComplete(p.round,assembled);}else showNack(p.round,suggest);","const{bits:missingBits,missing}=makeMissingBitmap(),suggest=suggestedRepairBits(missing,lastBits),gained=Math.max(0,rx.chunks.size-rx.passStartCount),roundMs=rx.roundStartedAt?performance.now()-rx.roundStartedAt:0;rx.roundStats.push({round:p.round+1,gained,missing,bits:lastBits,roundMs});emitMetric('passend',{round:p.round+1,gained,missing,bits:lastBits,suggest,roundMs,count:rx.chunks.size,total:rx.total,controlMode:rx.controlMode});rlog(`PASS_END R${p.round+1}: +${gained} bloques · ${rx.chunks.size}/${rx.total} · faltan ${missing}.`);rx.passStartCount=rx.chunks.size;if(rx.controlMode==='acoustic'){if(missing===0){const assembled=assembleFile();if(!assembled){rlog('CRC/ensamblado falló; enviando NACK HAC5 robusto.');rx.chunks.clear();$('rxFrames').textContent='0';$('rxMissing').textContent=rx.total;$('rxBar').style.width='0%';showNackAcoustic(p.round,2);}else showComplete(p.round,assembled);}else showNackAcoustic(p.round,suggest);}else{stopCamera();if(missing===0){const assembled=assembleFile();if(!assembled){rlog('CRC/ensamblado falló; solicitando retransmisión robusta completa.');rx.chunks.clear();$('rxFrames').textContent='0';$('rxMissing').textContent=rx.total;$('rxBar').style.width='0%';showNack(p.round,2);}else showComplete(p.round,assembled);}else showNack(p.round,suggest);}}");
  src=replaceOne(src,'acoustic-nack-function',"function showNack(round,suggestBits){rxState=RXS.NACK;","async function showNackAcoustic(round,suggestBits){rxState=RXS.NACK;rx.nextBits=suggestBits;const info=makeMissingBitmap(),missingList=parseMissingBitmap(info.bits,rx.total),missing=missingList.length;setPhase(`RECEPTOR · TX HAC5 NACK ${missing}`,'mid');rlog(`HAC5 NACK · ${missing} faltantes · enviando automáticamente.`);emitMetric('nack',{round:round+1,missing,suggestBits,closingBurst:missing>0&&missing<=500,cycles:missing>0&&missing<=500?3:1,controlMode:'acoustic'});try{await window.HopperAcoustic.sendNack({session:rx.session,round,bits:suggestBits,total:rx.total,fileCrc:rx.fileCrc,missing:missingList});rxState=RXS.WAIT_REPAIR;rx.passStartCount=rx.chunks.size;setPhase(`RECEPTOR · ESPERANDO REPAIR AUTOMÁTICO ${suggestBits}-BIT`,'on');}catch(e){rlog(`ERROR HAC5 NACK: ${e.message}`);stopCamera();showNack(round,suggestBits);}}\nfunction showNack(round,suggestBits){rxState=RXS.NACK;");
  src=replaceOne(src,'nack-event',"rlog(`NACK HPS7 · ${missing} faltantes · reparación sugerida ${suggestBits}-bit.`);}","rlog(`NACK HPS7 · ${missing} faltantes · reparación sugerida ${suggestBits}-bit.`);emitMetric('nack',{round:round+1,missing,suggestBits,closingBurst:missing>0&&missing<=500,cycles:missing>0&&missing<=500?3:1,controlMode:'manual'});}");
  src=replaceOne(src,'complete-event',"function showComplete(round,assembled){rxState=RXS.COMPLETE;rx.complete=assembled;const payload=concat(new Uint8Array([rx.dataGrid,rx.lastBits]),new Uint8Array(u32(rx.total)),new Uint8Array(u32(crc32(assembled.data)))),packets=splitControl(TYPE.COMPLETE,round,payload);renderReceived(assembled);openOverlay('COMPLETE','CRC final verificado. Déjalo hasta confirmación del emisor.','Emisor confirmó · Finalizar receptor',()=>{closeOverlay();rxState=RXS.DONE;setPhase('RECEPTOR · COMPLETADO','on');});repeatControl(packets,()=>`COMPLETE · ${rx.session.toString(16)}`,600);setPhase('RECEPTOR · COMPLETE','on');}","function showComplete(round,assembled){rxState=RXS.COMPLETE;rx.complete=assembled;rx.completedAt=performance.now();const dataMs=rx.firstDataAt?rx.completedAt-rx.firstDataAt:0,sessionMs=rx.sessionStartedAt?rx.completedAt-rx.sessionStartedAt:dataMs,avgBps=dataMs>0?assembled.data.length/(dataMs/1000):0,finalCrc=crc32(assembled.data);renderReceived(assembled);emitMetric('complete',{fileSize:assembled.data.length,total:rx.total,round:round+1,repairs:round,errors:rx.errors,validPackets:rx.validDataPackets,duplicateBlocks:rx.duplicateBlocks,dataMs,sessionMs,avgBps,peakBps:rx.peakByteRate,roundStats:rx.roundStats.slice(),bits:rx.lastBits,grid:rx.dataGrid,crc:finalCrc,baseChunk:BASE_CHUNK,controlMode:rx.controlMode});if(rx.controlMode==='acoustic'){setPhase('RECEPTOR · TX HAC5 COMPLETE','mid');window.HopperAcoustic.sendComplete({session:rx.session,round,bits:rx.lastBits,total:rx.total,fileCrc:finalCrc}).then(()=>{stopCamera();rxState=RXS.DONE;setPhase('RECEPTOR · COMPLETADO · CRC OK','on');rlog('COMPLETE HAC5 enviado y archivo verificado.');}).catch(e=>{rlog(`ERROR HAC5 COMPLETE: ${e.message}`);stopCamera();});return;}const payload=concat(new Uint8Array([rx.dataGrid,rx.lastBits]),new Uint8Array(u32(rx.total)),new Uint8Array(u32(finalCrc))),packets=splitControl(TYPE.COMPLETE,round,payload);openOverlay('COMPLETE','CRC final verificado. Déjalo hasta confirmación del emisor.','Emisor confirmó · Finalizar receptor',()=>{closeOverlay();rxState=RXS.DONE;setPhase('RECEPTOR · COMPLETADO','on');});repeatControl(packets,()=>`COMPLETE · ${rx.session.toString(16)}`,600);setPhase('RECEPTOR · COMPLETE','on');}");
  src=replaceOne(src,'live-metrics',"function updateMetrics(){const count=rx.chunks.size,total=rx.total,ts=performance.now();if(!count||!total)return;if(!rx.startedAt){rx.startedAt=ts;rx.lastTs=ts;rx.lastCount=count;}const elapsed=(ts-rx.startedAt)/1000;if(count>rx.lastCount&&ts>rx.lastTs){const inst=(count-rx.lastCount)/((ts-rx.lastTs)/1000);rx.emaRate=rx.emaRate?rx.emaRate*.72+inst*.28:inst;rx.lastCount=count;rx.lastTs=ts;}const rate=rx.emaRate||count/Math.max(.25,elapsed),byteRate=rate*BASE_CHUNK;$('rxElapsed').textContent=fmtTime(elapsed);$('rxRate').textContent=rate?`${rate.toFixed(rate<20?1:0)} blk/s · ${(byteRate/1024).toFixed(1)} KiB/s`:'—';$('rxEta').textContent=count>=total?'0s':rate?`≈ ${fmtTime((total-count)/rate)}`:'—';}","function updateMetrics(){const count=rx.chunks.size,total=rx.total,ts=performance.now();if(!count||!total)return;if(!rx.startedAt){rx.startedAt=ts;rx.lastTs=ts;rx.lastCount=count;rx.lastBytes=rx.bytesReceived;}const elapsed=(ts-rx.startedAt)/1000;if(rx.bytesReceived>rx.lastBytes&&ts>rx.lastTs){const instBps=(rx.bytesReceived-rx.lastBytes)/((ts-rx.lastTs)/1000);rx.byteRateEma=rx.byteRateEma?rx.byteRateEma*.72+instBps*.28:instBps;rx.peakByteRate=Math.max(rx.peakByteRate,instBps);rx.lastCount=count;rx.lastBytes=rx.bytesReceived;rx.lastTs=ts;}const byteRate=rx.byteRateEma||rx.bytesReceived/Math.max(.25,elapsed),rate=byteRate/BASE_CHUNK;$('rxElapsed').textContent=fmtTime(elapsed);$('rxRate').textContent=byteRate?`${rate.toFixed(rate<20?1:0)} blk/s · ${(byteRate/1024).toFixed(1)} KiB/s`:'—';$('rxEta').textContent=count>=total?'0s':byteRate?`≈ ${fmtTime(((total-count)*BASE_CHUNK)/byteRate)}`:'—';emitMetric('metrics',{count,total,elapsed,byteRate,peakBps:rx.peakByteRate,errors:rx.errors,validPackets:rx.validDataPackets,duplicateBlocks:rx.duplicateBlocks,round:rx.round+1,bytesReceived:rx.bytesReceived,baseChunk:BASE_CHUNK});}");
  src=replaceOne(src,'direct-binary-geometry',"function detectFiducials(frame){const st=photoStats(frame);","function detectFiducials(frame){const st=photoStats(frame);const bridge=window.__hopperBinaryTagBridge?.last;if(bridge?.valid&&bridge.markers){const src=MARKER_KEYS.map(k=>({x:MARKER_NORM[k][0],y:MARKER_NORM[k][1]})),dst=MARKER_KEYS.map(k=>bridge.markers[k]),H=computeHomography(src,dst);if(H){lastPhoto=st;return{found:4,markers:bridge.markers,H,quality:bridge.quality||94,photo:st};}}");
  src=replaceOne(src,'robust-cell-sampling',"function rgbAt(frame,x,y,rad){const xi=Math.round(x),yi=Math.round(y);if(xi<0||yi<0||xi>=frame.w||yi>=frame.h)return[0,0,0];let r=0,g=0,b=0,n=0;for(let yy=Math.max(0,yi-rad);yy<=Math.min(frame.h-1,yi+rad);yy++)for(let xx=Math.max(0,xi-rad);xx<=Math.min(frame.w-1,xi+rad);xx++){const p=(yy*frame.w+xx)*4;r+=frame.data[p];g+=frame.data[p+1];b+=frame.data[p+2];n++;}return[r/n,g/n,b/n];}","function rgbAt(frame,x,y,rad){const xi=Math.round(x),yi=Math.round(y);if(xi<0||yi<0||xi>=frame.w||yi>=frame.h)return[0,0,0];const s=Math.max(1,rad),pts=[[0,0],[-s,0],[s,0],[0,-s],[0,s]],rs=[],gs=[],bs=[];for(const[dx,dy]of pts){const xx=clamp(xi+dx,0,frame.w-1),yy=clamp(yi+dy,0,frame.h-1),p=(yy*frame.w+xx)*4;rs.push(frame.data[p]);gs.push(frame.data[p+1]);bs.push(frame.data[p+2]);}rs.sort((a,b)=>a-b);gs.sort((a,b)=>a-b);bs.sort((a,b)=>a-b);return[rs[2],gs[2],bs[2]];}");
  src=replaceOne(src,'camera-exposure-governor',"async function tuneCamera(track){try{const cap=track?.getCapabilities?.()||{},advanced={};if(Array.isArray(cap.focusMode)&&cap.focusMode.includes('continuous'))advanced.focusMode='continuous';if(Array.isArray(cap.exposureMode)&&cap.exposureMode.includes('continuous'))advanced.exposureMode='continuous';if(Array.isArray(cap.whiteBalanceMode)&&cap.whiteBalanceMode.includes('continuous'))advanced.whiteBalanceMode='continuous';if(Object.keys(advanced).length)await track.applyConstraints({advanced:[advanced]});}catch{}}","async function tuneCamera(track){try{const cap=track?.getCapabilities?.()||{},advanced={};if(Array.isArray(cap.focusMode)&&cap.focusMode.includes('continuous'))advanced.focusMode='continuous';if(Array.isArray(cap.exposureMode)&&cap.exposureMode.includes('continuous'))advanced.exposureMode='continuous';if(Array.isArray(cap.whiteBalanceMode)&&cap.whiteBalanceMode.includes('continuous'))advanced.whiteBalanceMode='continuous';if(cap.exposureCompensation&&Number.isFinite(cap.exposureCompensation.min)&&Number.isFinite(cap.exposureCompensation.max)){advanced.exposureCompensation=clamp(-0.7,cap.exposureCompensation.min,cap.exposureCompensation.max);}if(Object.keys(advanced).length)await track.applyConstraints({advanced:[advanced]});emitMetric('camera-tuned',{capabilities:cap,settings:track?.getSettings?.()||{},exposureCompensation:advanced.exposureCompensation??null});}catch{}}");
  src=replaceOne(src,'cancel-acoustic',"function cancelProtocol(reset=true){txRunToken++;stopCamera();closeOverlay();releaseWake();controlRx=null;","function cancelProtocol(reset=true){txRunToken++;stopCamera();closeOverlay();releaseWake();controlRx=null;if(reset)window.HopperAcoustic?.stopMic?.();");
  return src;
}

function loadScript(src){return new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=src;s.onload=resolve;s.onerror=()=>reject(new Error(`No se pudo cargar ${src}`));document.head.appendChild(s);});}
async function boot(){try{const res=await fetch(CORE_URL,{cache:'no-store'});if(!res.ok)throw new Error(`HPS7 core HTTP ${res.status}`);const original=await res.text();const patched=patchCore(original);new Function(`${patched}\n//# sourceURL=hps7-core-v090.js`)();window.__hopperRuntime={version:'0.9.7',closingBurstThreshold:500,closingBurstCycles:3,stableFps:12,baseChunk:372,vsyncPacing:true,microCellGuard:true,directBinaryGeometry:true,robustMedianSampling:true,acousticControl:'HAC5',patched:true};await loadScript('./color-warmup.js?v=090');await loadScript('./transfer-telemetry.js?v=090');window.dispatchEvent(new CustomEvent('hopper:runtime-ready',{detail:window.__hopperRuntime}));}catch(err){console.error(err);const phase=document.getElementById('phaseStatus');if(phase){phase.textContent='ERROR HPS7 RUNTIME';phase.className='chip off';}const log=document.getElementById('sendLog');if(log)log.textContent=`ERROR v0.9.7: ${err.message}\n`+log.textContent;}}
boot();
})();