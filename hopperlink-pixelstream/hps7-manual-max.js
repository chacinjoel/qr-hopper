(() => {
'use strict';

const CORE_URL='./hps7.js?v=090core';

function replaceOne(src,label,from,to){
  if(!src.includes(from)) throw new Error(`HPS7 Manual Max patch missing: ${label}`);
  const out=src.replace(from,to);
  if(out===src) throw new Error(`HPS7 Manual Max patch failed: ${label}`);
  return out;
}

function patchCore(src){
  src=replaceOne(src,'367-byte-blocks',"const BASE_CHUNK = 360;","const BASE_CHUNK = 367;");
  src=replaceOne(src,'closing-burst-500',"function repairPasses(n){return n<=100?3:n<=300?2:1;}","function repairPasses(n){return n>0&&n<=500?3:1;}");
  src=replaceOne(src,'fixed-modulation',"function modulationSelection(){const v=$('modulationMode')?.value||'auto4';if(v==='gray2')return{bits:2,auto:false,label:'Robust Gray 2-bit'};if(v==='color3')return{bits:3,auto:false,label:'Color 3-bit'};return{bits:4,auto:true,label:'Adaptive Color 4-bit'};}","function modulationSelection(){const v=$('modulationMode')?.value||'color3';if(v==='gray2')return{bits:2,auto:false,label:'Max Gray 2-bit'};if(v==='color3')return{bits:3,auto:false,label:'Max Color 3-bit'};return{bits:4,auto:false,label:'Max Color 4-bit'};}");
  src=replaceOne(src,'calibrated-speed',"function speedProfile(){return SPEED[$('speedMode')?.value]||SPEED.turbo;}","function speedProfile(){const key=$('speedMode')?.value||'maxcal';if(key==='maxcal'){const bits=modulationSelection().bits;if(bits===2)return{label:'2-bit Max',fps:15,repeat:1,hint:'Max calibrado 2-bit · 15 img/s · 2 bloques por imagen · robustez gris.'};if(bits===3)return{label:'3-bit Max',fps:12,repeat:1,hint:'Max calibrado 3-bit · 12 img/s · 3 bloques por imagen · punto estable recomendado.'};return{label:'4-bit Max',fps:10,repeat:1,hint:'Max calibrado 4-bit · 10 img/s · 4 bloques por imagen · más tiempo por frame para 16 símbolos.'};}return SPEED[key]||SPEED.balanced;}");
  src=replaceOne(src,'runtime-helpers',"function clamp(v,a,b){return Math.max(a,Math.min(b,v));}","function clamp(v,a,b){return Math.max(a,Math.min(b,v));}\nfunction emitMetric(name,detail={}){try{window.dispatchEvent(new CustomEvent('hopper:'+name,{detail}));}catch{}}\nfunction opticalHold(ms){return new Promise(resolve=>{const start=performance.now();const step=t=>{if(t-start>=ms-1)resolve();else requestAnimationFrame(step);};requestAnimationFrame(step);});}\nfunction repairPrerollMs(bits){return bits===2?700:bits===3?1200:1800;}");
  src=replaceOne(src,'rx-telemetry-state',"function freshRx(){return{session:null,total:0,dataGrid:56,preferredBits:4,lastBits:4,nextBits:null,autoMod:true,fileCrc:0,fileSize:0,chunks:new Map(),errors:0,lastPassRound:-1,round:0,startedAt:0,lastCount:0,lastTs:0,emaRate:0,complete:null,passStartCount:0,lastPassReceived:0};}","function freshRx(){return{session:null,total:0,dataGrid:56,preferredBits:4,lastBits:4,nextBits:null,autoMod:false,fileCrc:0,fileSize:0,chunks:new Map(),errors:0,lastPassRound:-1,round:0,startedAt:0,lastCount:0,lastTs:0,emaRate:0,complete:null,passStartCount:0,lastPassReceived:0,sessionStartedAt:0,firstDataAt:0,completedAt:0,bytesReceived:0,lastBytes:0,byteRateEma:0,peakByteRate:0,validDataPackets:0,duplicateBlocks:0,roundStartedAt:0,lastDataRound:-1,roundStats:[]};}");
  src=replaceOne(src,'micro-cell-guard',"function renderPacket(raw,grid,bits){const c=$('pixelCanvas');c.width=grid;c.height=grid;const ctx=c.getContext('2d',{alpha:false}),sym=rawToSymbols(raw,grid,bits),pal=palette(bits),img=ctx.createImageData(grid,grid);for(let i=0;i<sym.length;i++){const rgb=pal[sym[i]]||pal[0],p=i*4;img.data[p]=rgb[0];img.data[p+1]=rgb[1];img.data[p+2]=rgb[2];img.data[p+3]=255;}ctx.putImageData(img,0,0);}","function renderPacket(raw,grid,bits){const c=$('pixelCanvas'),sym=rawToSymbols(raw,grid,bits),pal=palette(bits);if(bits===2){c.width=grid;c.height=grid;const ctx=c.getContext('2d',{alpha:false}),img=ctx.createImageData(grid,grid);for(let i=0;i<sym.length;i++){const rgb=pal[sym[i]]||pal[0],p=i*4;img.data[p]=rgb[0];img.data[p+1]=rgb[1];img.data[p+2]=rgb[2];img.data[p+3]=255;}ctx.putImageData(img,0,0);return;}const S=8,P=1,W=grid*S;c.width=W;c.height=W;const ctx=c.getContext('2d',{alpha:false}),img=ctx.createImageData(W,W),d=img.data;for(let p=0;p<d.length;p+=4){d[p]=58;d[p+1]=58;d[p+2]=58;d[p+3]=255;}for(let gy=0;gy<grid;gy++)for(let gx=0;gx<grid;gx++){const rgb=pal[sym[gy*grid+gx]]||pal[0],x0=gx*S+P,y0=gy*S+P;for(let y=y0;y<(gy+1)*S-P;y++)for(let x=x0;x<(gx+1)*S-P;x++){const p=(y*W+x)*4;d[p]=rgb[0];d[p+1]=rgb[1];d[p+2]=rgb[2];}}ctx.putImageData(img,0,0);}");
  src=replaceOne(src,'closing-burst-order',"for(let pass=1;pass<=passes;pass++){const repeats=pass===1?sp.repeat:1;for(let gi=0;gi<groups.length;gi++){const g=groups[gi],raw=makePacket(TYPE.DATA,tx.session,round,g.first,tx.total,g.payload,tx.grid,bits);","for(let pass=1;pass<=passes;pass++){const repeats=pass===1?sp.repeat:1,passGroups=pass===1?groups:(pass===2?groups.slice().reverse():groups.filter((_,i)=>i%2===0).concat(groups.filter((_,i)=>i%2===1)));if(isRepair&&passes===3){slog(`Closing Burst ${pass}/3 · ${indices.length} faltantes · ${pass===1?'normal':pass===2?'invertido':'intercalado'} · SIN NACK intermedio.`);emitMetric('closing-cycle',{cycle:pass,cycles:3,missing:indices.length,bits});}for(let gi=0;gi<passGroups.length;gi++){const g=passGroups[gi],raw=makePacket(TYPE.DATA,tx.session,round,g.first,tx.total,g.payload,tx.grid,bits);");
  src=replaceOne(src,'vsync-pacing',"await sleep(frameMs);}}}","await opticalHold(frameMs);}}}");
  src=replaceOne(src,'repair-preroll',"$('streamMeta').textContent=`REPAIR ${bits}-bit · primer paquete fijo 3s`;$('streamBottom').textContent=`${g.count} bloque(s) lógicos visibles mientras la cámara estabiliza exposición/color.`;await sleep(3000);","const pre=repairPrerollMs(bits);$('streamMeta').textContent=`REPAIR ${bits}-bit · lock inicial ${(pre/1000).toFixed(1)}s`;$('streamBottom').textContent=`${g.count} bloque(s) lógicos visibles mientras la cámara recupera exposición/foco.`;await opticalHold(pre);");
  src=replaceOne(src,'fast-control-cycle',"function repeatControl(packets,metaFn,period=450){","function repeatControl(packets,metaFn,period=350){");
  src=replaceOne(src,'direct-binary-geometry',"function detectFiducials(frame){const st=photoStats(frame);","function detectFiducials(frame){const st=photoStats(frame);const bridge=window.__hopperBinaryTagBridge?.last;if(bridge?.valid&&bridge.markers&&performance.now()-bridge.ts<260){const src=MARKER_KEYS.map(k=>({x:MARKER_NORM[k][0],y:MARKER_NORM[k][1]})),dst=MARKER_KEYS.map(k=>bridge.markers[k]),H=computeHomography(src,dst);if(H){lastPhoto=st;return{found:4,markers:bridge.markers,H,quality:bridge.quality||94,photo:st};}}");
  src=replaceOne(src,'robust-cell-sampling',"function rgbAt(frame,x,y,rad){const xi=Math.round(x),yi=Math.round(y);if(xi<0||yi<0||xi>=frame.w||yi>=frame.h)return[0,0,0];let r=0,g=0,b=0,n=0;for(let yy=Math.max(0,yi-rad);yy<=Math.min(frame.h-1,yi+rad);yy++)for(let xx=Math.max(0,xi-rad);xx<=Math.min(frame.w-1,xi+rad);xx++){const p=(yy*frame.w+xx)*4;r+=frame.data[p];g+=frame.data[p+1];b+=frame.data[p+2];n++;}return[r/n,g/n,b/n];}","function rgbAt(frame,x,y,rad){const xi=Math.round(x),yi=Math.round(y);if(xi<0||yi<0||xi>=frame.w||yi>=frame.h)return[0,0,0];const s=Math.max(1,rad),pts=[[0,0],[-s,0],[s,0],[0,-s],[0,s]],rs=[],gs=[],bs=[];for(const[dx,dy]of pts){const xx=clamp(xi+dx,0,frame.w-1),yy=clamp(yi+dy,0,frame.h-1),p=(yy*frame.w+xx)*4;rs.push(frame.data[p]);gs.push(frame.data[p+1]);bs.push(frame.data[p+2]);}rs.sort((a,b)=>a-b);gs.sort((a,b)=>a-b);bs.sort((a,b)=>a-b);return[rs[2],gs[2],bs[2]];}");
  src=replaceOne(src,'less-full-redetect',"if(trackedSuccess%10===0){","if(trackedSuccess%30===0){");
  src=replaceOne(src,'capture-budget',"function captureFrame(){const v=$('video'),c=$('capture');if(!v.videoWidth)return null;const maxW=1080,scale=Math.min(1,maxW/v.videoWidth),w=Math.max(320,Math.round(v.videoWidth*scale)),h=Math.max(240,Math.round(v.videoHeight*scale));","function captureFrame(){const v=$('video'),c=$('capture');if(!v.videoWidth)return null;const activeBits=cameraMode==='senderControl'?2:(rx?.session?(rx.nextBits||rx.lastBits):3),maxW=activeBits===2?820:activeBits===3?900:1000,scale=Math.min(1,maxW/v.videoWidth),w=Math.max(320,Math.round(v.videoWidth*scale)),h=Math.max(240,Math.round(v.videoHeight*scale));");
  src=replaceOne(src,'camera-request-budget',"async function openCameraStream(){const attempts=[{video:{facingMode:{ideal:'environment'},width:{ideal:1920},height:{ideal:1080},frameRate:{ideal:30,min:15}},audio:false},{video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:720},frameRate:{ideal:30}},audio:false},{video:{facingMode:{ideal:'environment'}},audio:false}];","async function openCameraStream(){const attempts=[{video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:720},frameRate:{ideal:30,min:24}},audio:false},{video:{facingMode:{ideal:'environment'},width:{ideal:1920},height:{ideal:1080},frameRate:{ideal:30,min:15}},audio:false},{video:{facingMode:{ideal:'environment'}},audio:false}];");
  src=replaceOne(src,'camera-exposure',"async function tuneCamera(track){try{const cap=track?.getCapabilities?.()||{},advanced={};if(Array.isArray(cap.focusMode)&&cap.focusMode.includes('continuous'))advanced.focusMode='continuous';if(Array.isArray(cap.exposureMode)&&cap.exposureMode.includes('continuous'))advanced.exposureMode='continuous';if(Array.isArray(cap.whiteBalanceMode)&&cap.whiteBalanceMode.includes('continuous'))advanced.whiteBalanceMode='continuous';if(Object.keys(advanced).length)await track.applyConstraints({advanced:[advanced]});}catch{}}","async function tuneCamera(track){try{const cap=track?.getCapabilities?.()||{},advanced={};if(Array.isArray(cap.focusMode)&&cap.focusMode.includes('continuous'))advanced.focusMode='continuous';if(Array.isArray(cap.exposureMode)&&cap.exposureMode.includes('continuous'))advanced.exposureMode='continuous';if(Array.isArray(cap.whiteBalanceMode)&&cap.whiteBalanceMode.includes('continuous'))advanced.whiteBalanceMode='continuous';if(cap.exposureCompensation&&Number.isFinite(cap.exposureCompensation.min)&&Number.isFinite(cap.exposureCompensation.max))advanced.exposureCompensation=clamp(-0.5,cap.exposureCompensation.min,cap.exposureCompensation.max);if(Object.keys(advanced).length)await track.applyConstraints({advanced:[advanced]});emitMetric('camera-tuned',{exposureCompensation:advanced.exposureCompensation??null,settings:track?.getSettings?.()||{}});}catch{}}");
  src=replaceOne(src,'reset-session-telemetry',"function resetRxSession(session,total,dataGrid,bits,autoMod,fileCrc=0,fileSize=0){rx=freshRx();rx.session=session;rx.total=total;rx.dataGrid=dataGrid;rx.preferredBits=bits;rx.lastBits=bits;rx.autoMod=autoMod;rx.fileCrc=fileCrc;rx.fileSize=fileSize;","function resetRxSession(session,total,dataGrid,bits,autoMod,fileCrc=0,fileSize=0){rx=freshRx();rx.session=session;rx.total=total;rx.dataGrid=dataGrid;rx.preferredBits=bits;rx.lastBits=bits;rx.autoMod=false;rx.fileCrc=fileCrc;rx.fileSize=fileSize;rx.sessionStartedAt=performance.now();");
  src=replaceOne(src,'hello-telemetry',"setPhase(`RECEPTOR · LOCK ${bits}-BIT`,'on');}","setPhase(`RECEPTOR · LOCK ${bits}-BIT`,'on');emitMetric('hello',{session:p.session,total:p.total,fileSize,dataGrid,bits,autoMod:false,baseChunk:BASE_CHUNK,controlMode:'manual'});}");
  src=replaceOne(src,'data-telemetry',"function handleData(p){if(cameraMode!=='receiverData'||rx.session===null)return;if(p.session!==rx.session||p.total!==rx.total||p.grid!==rx.dataGrid||!BITS_OPTIONS.includes(p.bits)){rx.errors++;updateErrors();return;}const bundle=parseBundle(p.payload,rx.total);if(!bundle){rx.errors++;updateErrors();return;}rxState=RXS.RECEIVING;rx.round=Math.max(rx.round,p.round);rx.lastBits=p.bits;rx.nextBits=null;let added=0;for(const e of bundle)if(!rx.chunks.has(e.idx)){rx.chunks.set(e.idx,e.data);added++;}if(added){$('rxFrames').textContent=rx.chunks.size;$('rxMissing').textContent=Math.max(0,rx.total-rx.chunks.size);$('rxBar').style.width=((rx.chunks.size/rx.total)*100).toFixed(1)+'%';updateMetrics();}setPhase(`RECEPTOR · ${p.bits}-BIT · ${rx.chunks.size}/${rx.total}`,'on');}","function handleData(p){if(cameraMode!=='receiverData'||rx.session===null)return;if(p.session!==rx.session||p.total!==rx.total||p.grid!==rx.dataGrid||!BITS_OPTIONS.includes(p.bits)){rx.errors++;updateErrors();emitMetric('reject',{errors:rx.errors,reason:'identity'});return;}const bundle=parseBundle(p.payload,rx.total);if(!bundle){rx.errors++;updateErrors();emitMetric('reject',{errors:rx.errors,reason:'bundle'});return;}rxState=RXS.RECEIVING;rx.round=Math.max(rx.round,p.round);rx.lastBits=p.bits;rx.nextBits=null;const ts=performance.now();if(!rx.firstDataAt)rx.firstDataAt=ts;if(rx.lastDataRound!==p.round){rx.lastDataRound=p.round;rx.roundStartedAt=ts;}let added=0,addedBytes=0,dups=0;for(const e of bundle){if(!rx.chunks.has(e.idx)){rx.chunks.set(e.idx,e.data);added++;addedBytes+=e.data.length;}else dups++;}rx.validDataPackets++;rx.duplicateBlocks+=dups;if(added){rx.bytesReceived+=addedBytes;$('rxFrames').textContent=rx.chunks.size;$('rxMissing').textContent=Math.max(0,rx.total-rx.chunks.size);$('rxBar').style.width=((rx.chunks.size/rx.total)*100).toFixed(1)+'%';updateMetrics();}emitMetric('data',{round:p.round+1,bits:p.bits,added,addedBytes,duplicates:dups,count:rx.chunks.size,total:rx.total,bytesReceived:rx.bytesReceived,validPackets:rx.validDataPackets,errors:rx.errors});setPhase(`RECEPTOR · ${p.bits}-BIT · ${rx.chunks.size}/${rx.total}`,'on');}");
  src=replaceOne(src,'passend-telemetry',"rlog(`PASS_END R${p.round+1}: +${gained} bloques · ${rx.chunks.size}/${rx.total} · faltan ${missing} · siguiente ${suggest}-bit.`);rx.passStartCount=rx.chunks.size;","const roundMs=rx.roundStartedAt?performance.now()-rx.roundStartedAt:0;rx.roundStats.push({round:p.round+1,gained,missing,bits:lastBits,roundMs});emitMetric('passend',{round:p.round+1,gained,missing,bits:lastBits,suggest:lastBits,roundMs,count:rx.chunks.size,total:rx.total,controlMode:'manual'});rlog(`PASS_END R${p.round+1}: +${gained} bloques · ${rx.chunks.size}/${rx.total} · faltan ${missing}.`);rx.passStartCount=rx.chunks.size;");
  src=replaceOne(src,'nack-telemetry',"rlog(`NACK HPS7 · ${missing} faltantes · reparación sugerida ${suggestBits}-bit.`);}","rlog(`NACK HPS7 · ${missing} faltantes · reparación ${suggestBits}-bit${missing<=500?' · CLOSING BURST ×3':''}.`);emitMetric('nack',{round:round+1,missing,suggestBits,closingBurst:missing>0&&missing<=500,cycles:missing>0&&missing<=500?3:1,controlMode:'manual'});}");
  src=replaceOne(src,'live-metrics',"function updateMetrics(){const count=rx.chunks.size,total=rx.total,ts=performance.now();if(!count||!total)return;if(!rx.startedAt){rx.startedAt=ts;rx.lastTs=ts;rx.lastCount=count;}const elapsed=(ts-rx.startedAt)/1000;if(count>rx.lastCount&&ts>rx.lastTs){const inst=(count-rx.lastCount)/((ts-rx.lastTs)/1000);rx.emaRate=rx.emaRate?rx.emaRate*.72+inst*.28:inst;rx.lastCount=count;rx.lastTs=ts;}const rate=rx.emaRate||count/Math.max(.25,elapsed),byteRate=rate*BASE_CHUNK;$('rxElapsed').textContent=fmtTime(elapsed);$('rxRate').textContent=rate?`${rate.toFixed(rate<20?1:0)} blk/s · ${(byteRate/1024).toFixed(1)} KiB/s`:'—';$('rxEta').textContent=count>=total?'0s':rate?`≈ ${fmtTime((total-count)/rate)}`:'—';}","function updateMetrics(){const count=rx.chunks.size,total=rx.total,ts=performance.now();if(!count||!total)return;if(!rx.startedAt){rx.startedAt=ts;rx.lastTs=ts;rx.lastCount=count;rx.lastBytes=rx.bytesReceived;}const elapsed=(ts-rx.startedAt)/1000;if(rx.bytesReceived>rx.lastBytes&&ts>rx.lastTs){const inst=(rx.bytesReceived-rx.lastBytes)/((ts-rx.lastTs)/1000);rx.byteRateEma=rx.byteRateEma?rx.byteRateEma*.72+inst*.28:inst;rx.peakByteRate=Math.max(rx.peakByteRate,inst);rx.lastBytes=rx.bytesReceived;rx.lastCount=count;rx.lastTs=ts;}const byteRate=rx.byteRateEma||rx.bytesReceived/Math.max(.25,elapsed),rate=byteRate/BASE_CHUNK;$('rxElapsed').textContent=fmtTime(elapsed);$('rxRate').textContent=byteRate?`${rate.toFixed(rate<20?1:0)} blk/s · ${(byteRate/1024).toFixed(1)} KiB/s`:'—';$('rxEta').textContent=count>=total?'0s':byteRate?`≈ ${fmtTime(((total-count)*BASE_CHUNK)/byteRate)}`:'—';emitMetric('metrics',{count,total,elapsed,byteRate,peakBps:rx.peakByteRate,errors:rx.errors,validPackets:rx.validDataPackets,duplicateBlocks:rx.duplicateBlocks,round:rx.round+1,bytesReceived:rx.bytesReceived,baseChunk:BASE_CHUNK});}");
  src=replaceOne(src,'complete-telemetry',"function showComplete(round,assembled){rxState=RXS.COMPLETE;rx.complete=assembled;const payload=concat(new Uint8Array([rx.dataGrid,rx.lastBits]),new Uint8Array(u32(rx.total)),new Uint8Array(u32(crc32(assembled.data)))),packets=splitControl(TYPE.COMPLETE,round,payload);renderReceived(assembled);","function showComplete(round,assembled){rxState=RXS.COMPLETE;rx.complete=assembled;rx.completedAt=performance.now();const finalCrc=crc32(assembled.data),dataMs=rx.firstDataAt?rx.completedAt-rx.firstDataAt:0,sessionMs=rx.sessionStartedAt?rx.completedAt-rx.sessionStartedAt:dataMs,avgBps=dataMs>0?assembled.data.length/(dataMs/1000):0;emitMetric('complete',{fileSize:assembled.data.length,total:rx.total,round:round+1,repairs:round,errors:rx.errors,validPackets:rx.validDataPackets,duplicateBlocks:rx.duplicateBlocks,dataMs,sessionMs,avgBps,peakBps:rx.peakByteRate,roundStats:rx.roundStats.slice(),bits:rx.lastBits,grid:rx.dataGrid,crc:finalCrc,baseChunk:BASE_CHUNK,controlMode:'manual'});const payload=concat(new Uint8Array([rx.dataGrid,rx.lastBits]),new Uint8Array(u32(rx.total)),new Uint8Array(u32(finalCrc))),packets=splitControl(TYPE.COMPLETE,round,payload);renderReceived(assembled);");
  src=replaceOne(src,'startup-log',"slog('HPS7 cargado · bloques lógicos 360 B · modulación 2/3/4-bit · AutoLock fotométrico.');","slog('Manual Max v0.9.8 · 367 B/bloque · 2-bit=2 bloques@15 · 3-bit=3@12 · 4-bit=4@10 · Closing Burst ≤500 ×3 sin NACK intermedio.');");
  return src;
}

function loadScript(src){return new Promise((resolve,reject)=>{const s=document.createElement('script');s.src=src;s.onload=resolve;s.onerror=()=>reject(new Error(`No se pudo cargar ${src}`));document.head.appendChild(s);});}

async function boot(){
  try{
    const res=await fetch(CORE_URL,{cache:'no-store'});if(!res.ok)throw new Error(`HPS7 core HTTP ${res.status}`);
    const original=await res.text(),patched=patchCore(original);
    new Function(`${patched}\n//# sourceURL=hps7-core-v098.js`)();
    window.__hopperRuntime={version:'0.9.8',mode:'manual-max',baseChunk:367,closingBurstThreshold:500,closingBurstCycles:3,calibratedFps:{2:15,3:12,4:10},vsyncPacing:true,microCellGuard:true,directBinaryGeometry:true,robustMedianSampling:true,cameraBudget:true};
    await loadScript('./color-warmup.js?v=098');
    await loadScript('./transfer-telemetry.js?v=098');
    window.dispatchEvent(new CustomEvent('hopper:runtime-ready',{detail:window.__hopperRuntime}));
  }catch(err){
    console.error(err);const phase=document.getElementById('phaseStatus');if(phase){phase.textContent='ERROR MANUAL MAX';phase.className='chip off';}
    const log=document.getElementById('sendLog');if(log)log.textContent=`ERROR v0.9.8: ${err.message}\n`+log.textContent;
  }
}
boot();
})();
